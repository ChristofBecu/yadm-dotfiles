# Keybindings for controlling MOC (Music On Console) in i3 window manager

# Play/Pause toggle
bindsym $mod+Ctrl+Home exec mocp --toggle-pause

# Previous track
bindsym $mod+Ctrl+Left exec mocp --previous

# Next track
bindsym $mod+Ctrl+Right exec mocp --next
