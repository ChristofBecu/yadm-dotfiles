general {
        output_format= "i3bar"
        colors = true
        interval = 1
}

order += "read_file clipboard"
order += "read_file mocp"
#order += "battery all"
#order += "disk /"
#order += "memory"
#order += "wireless _first_"
#order += "tztime local"
order += "volume master"

wireless _first_ {
        #format_up = "W: (%quality at %essid) %ip"
        format_up = "📶 %quality at %essid"
        format_down = "🚫: down"
}

battery all {
        format = "%status/%percentage/%remaining"
}

disk "/" {
        format = "💾 %free"
}

memory {
        format = "📈 %used 🧠 %total"
        threshold_degraded = "10%"
        format_degraded = "⚠️ %free"
}

tztime local {
        format = "📅 %a, %d-%m-%Y 🕒 %H:%M:%S"
}

volume master {
        format = "🔊 %volume"
        format_muted = "🔇"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

read_file mocp {
    format = %content
    path = "~/logs/mocp.log"
    min_width = 50
    align = right
    separator_block_width = 20
}

read_file clipboard {
        format = %content
        path = "/tmp/clipboard_status"
        min_width = 50
        align = center
        separator_block_width = 30
}
